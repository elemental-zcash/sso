version: '3'
services:
  sso_flask_server:
    build: ./
    ports:
      - "3535:5050"
    hostname: sso_flask_server
    container_name: sso_flask_server
    networks:
      - elemental_net
networks:
  elemental_net:
    external: true
    driver: bridge
