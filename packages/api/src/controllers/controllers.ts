/**
 * Full controllers with validation, etc
 */
import userController from './user.controller';
import authCodeController from './auth-code.controller';

export {
  userController as users,
  authCodeController as authCodes,
};
