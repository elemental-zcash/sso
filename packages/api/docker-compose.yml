version: '3'
services:
  api:
    build: .
    environment:
      - PORT=8081
    networks:
      - elemental_net
    env_file:
      .env
    #volumes:
    #  - ./packages/api:/usr/src/app
    # links:
    #   - redis
      # - zecwallet_api
      # - database
    # depends_on:
    #   - nginx-proxy
networks:
  elemental_net:
    external: true
    driver: bridge
